json.extract! work, :id, :works_name, :worls_desc, :location, :type_works, :acount_woks, :start_date_works, :end_date_works, :employees_id, :created_at, :updated_at
json.url work_url(work, format: :json)
