<%= form_with(model: tool) do |form| %>
  <% if tool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tool.errors.count, "error") %> prohibited this tool from being saved:</h2>

      <ul>
        <% tool.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tools_name %>
    <%= form.text_field :tools_name %>
  </div>

  <div class="field">
    <%= form.label :tools_models %>
    <%= form.text_field :tools_models %>
  </div>

  <div class="field">
    <%= form.label :tools_brands %>
    <%= form.text_field :tools_brands %>
  </div>

  <div class="field">
    <%= form.label :date_acquistion %>
    <%= form.date_select :date_acquistion %>
  </div>

  <div class="field">
    <%= form.label :amount_storage %>
    <%= form.number_field :amount_storage %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
